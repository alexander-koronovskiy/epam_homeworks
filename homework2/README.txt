Итерация № 2

задача 1
========

1. Чтобы не возникло переполнение памяти при чтении файла - его содержимое буфферизировал.
При чтении содержимое разбивается на части и, а затем обрабатывается.

2. Пока не очень хорошо понимаю, как обработать слова, разделенные при переносе,
проверка на символы ascii не совсем корректно работает, кажется

3. Функцию-обработчик для текста вынес в отдельный файл. У некоторых функций переписал код

задача 2
===========
Реализовал сам, двумя различными способами

===========
требования
===========

задача 3
============
Твой код можно было бы записать как return list(itertools.product(*args))
Теперь жду твоей реализации без использования .product()

задача 4
============
Наверное уже догадался. Реализуй сам, это несложно.

задача 5
=============
Функция должна работать не только на символах, а на любых последовательностях с уникальными элементами.
Например, custom_range([1,2,3,4,5], 2, 4) должно выдать [3, 4]
